[project]
name = "organ_donation_erp"
version = "0.0.1"
description = "Full-suite ERP for Organ Donation and Transplantation Organizations"
requires-python = ">=3.10"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
